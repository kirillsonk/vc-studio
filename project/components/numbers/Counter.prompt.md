Animated tabular number (tokens, ₽, days). Ink by default; `tone="positive"` only for live/saved semantics.
```jsx
<Counter value={1284310} from={0} size="xl" />
<Counter value={cost} suffix=" ₽" size="md" />
```
Re-animates from the previous value on change; ru-RU grouping.
