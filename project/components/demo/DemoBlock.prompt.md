Frame for a real demo: poster → «Запустить демо» → loading → children. Caption sits under the frame, not over it. `dark` for the single contrast section.
```jsx
<DemoBlock title="3D-конфигуратор" meta="WebGL · 60 FPS" ratio="21/9"><canvas/></DemoBlock>
```
